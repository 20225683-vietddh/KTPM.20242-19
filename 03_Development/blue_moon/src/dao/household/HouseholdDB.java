package dao.household;

import java.time.LocalDate;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Observable;
import java.util.stream.Collectors;

import javafx.collections.ObservableList;
import models.Household;
import models.Resident;

public class HouseholdDB {

//	public final static List<Household> HOUSEHOLD_SAMPLE = new ArrayList<>(
//		
//		    Arrays.asList(
//		        new Household(1, "123 Le Loi, District 1", "Ho Chi Minh City", 3, "M001", "Nguyen Van A", "0901234567", "a.nguyen@example.com", "2020-05-10",
//		    		    MemberDB.MEMBER_SAMPLE.stream()
//		    		        .filter(m -> m.getHouseholdId() == 1)
//		    		        .collect(Collectors.toList())),
//		        new Household(2, "45A Nguyen Trai, District 5", "Ho Chi Minh City", 5, "M004", "Tran Thi B", "0902345678", "b.tran@example.com", "2021-03-15",
//		                MemberDB.MEMBER_SAMPLE.stream()
//		                    .filter(m -> m.getHouseholdId() == 2)
//		                    .collect(Collectors.toList())
//		            ),
//		            new Household(3, "89 Truong Chinh, District 12", "Ho Chi Minh City", 3, "M009",  "Le Van C", "0903456789", "c.le@example.com", "2019-07-08",
//		                MemberDB.MEMBER_SAMPLE.stream()
//		                    .filter(m -> m.getHouseholdId() == 3)
//		                    .collect(Collectors.toList())
//		            ),
//		            new Household(4, "76 Dien Bien Phu, District 3", "Ho Chi Minh City", 6, "M012", "Pham Thi D", "0904567890", "d.pham@example.com", "2022-01-12",
//		                MemberDB.MEMBER_SAMPLE.stream()
//		                    .filter(m -> m.getHouseholdId() == 4)
//		                    .collect(Collectors.toList())
//		            )
//		    )
//		);
	
	
}
